# 更新日志

## 0.13.1（2026-09-13）

### 新功能

- **复制卡片内容**：卡片操作区新增「复制」按钮（置顶左边），把笔记原文（纯 Markdown，含标签与图片引用）写入剪贴板；点击后按钮短暂显示「已复制 / 复制失败」反馈，1.5 秒复原
- **回顾弹窗同步支持**：随机回顾 / 每日回顾 / N 年前的今天弹窗底部同样有「复制」按钮，「换一条」后反馈状态自动复位

### 实现

- 新增 `clipboard.ts` 的 `copyText`，三层降级：① Tauri 剪贴板插件（`tauri-plugin-clipboard-manager`，Rust 侧写入，不受 WebView 剪贴板权限影响，真机主路径）→ ② `navigator.clipboard`（浏览器调试路径）→ ③ 临时 textarea + `execCommand("copy")` 兜底
- capabilities 增加 `clipboard-manager:allow-write-text` 权限；`tauri-mock.ts` 增加插件命令 case（浏览器里转发到标准剪贴板 API）
- 新增 4 项 `copyText` 单测（标准路径 / 插件被拒降级 / 双失败 / 兜底异常不崩溃）

## 0.13.0（2026-09-13）

### 新功能

- **格式工具栏**：主输入框与卡片编辑态的编辑区下方新增一排图标按钮——
  标签（#，插入即唤起自动补全）、加粗、行内代码、无序列表、有序列表、任务清单、插入图片
- 插入图片走系统文件选择框，上传与粘贴 / 拖入共用同一条链路（去重、10 MB 上限一致）
- 加粗 / 行内代码 / 列表按钮在选区已应用对应格式时呈主题色激活态，再点一次取消
- 全部按钮不抢编辑器焦点（mousedown 拦截）：点击后命令作用于当前光标 / 选区，打字不中断

### 实现

- 工具栏内置于 `RichEditor`（`toolbar` prop 开关），两处编辑入口一行接入
- 图片上传逻辑从 `useImagePaste` 抽出为独立的 `uploadImageFile`，粘贴与按钮共用
- 图标为内联 SVG（currentColor 跟随主题深浅色）

## 0.12.0（2026-09-13）

### 新功能

- **所见即所得编辑器**：输入框与卡片编辑框整体迁移到 TipTap（v3），打字即时成型——
  `**加粗**`、`行内代码`、代码块、无序 / 有序列表、`- [ ] ` 任务复选框全部边打边变，
  不再需要「预览面板」，发送前看到的就是成品
  - 复选框在编辑器里直接点击勾选；列表回车自动延续；代码块围栏输入即成块
  - 图片粘贴 / 拖入后以真实图片内嵌显示，点击仍可放大
  - `#标签` 自动补全重做：输入 `#` 唤起候选，↑↓ / Enter / 鼠标点选均可
- **中文加粗修复**：TipTap v3 官方规则要求 `**` 前必须是行首或空白，
  「这是**加粗**文字」无法触发——补了自定义规则放行任意前缀（内容首尾非空格）

### 技术与数据

- 存储仍是纯 Markdown：新增 `tiptap-md.ts`（md ↔ ProseMirror 文档 JSON 双向纯函数），
  加载复用卡片渲染器同一套 `parseBlocks`，「打开→不动→保存」字节级无损（新增 9 项往返 / 幂等 / 语义不变测试）
- 相邻文本段落之间序列化补空行（标准 Markdown 语义），段落与软换行（Shift+Enter）严格区分；
  有序列表编号按渲染口径归一化；嵌套列表（渲染器不支持）保存时拍平
- 移除 0.11.0 的实时预览面板与旧 TagTextarea 高亮输入层（TagInput / TagTextarea 组件下线）
- 新依赖：`@tiptap/react` `@tiptap/starter-kit` `@tiptap/core` `@tiptap/extension-task-list`
  `@tiptap/extension-task-item` `@tiptap/extensions` `@tiptap/suggestion`

## 0.11.0（2026-09-13）

### 新功能

- **输入框实时预览**：页脚新增「预览」开关，开启后输入框下方出现预览面板，随输入
  实时渲染 Markdown 成品——加粗 / 行内代码 / 代码块 / 列表 / TODO 复选框 / 标签 / 图片全部覆盖
  - 与卡片正文共用同一套渲染器（`md.tsx`）和图片组件，预览即所得，发送前后零色差
  - 开关偏好存 localStorage，下次启动记住；输入框为空时面板自动隐藏
  - 渲染结果按内容记忆化（`useMemo`），连续输入不重复解析

## 0.10.0（2026-09-13）

### 新功能

- **整库加密（SQLCipher）**：数据库文件用 SQLCipher 4（AES-256）加密，WAL 文件、
  自动备份、恢复安全副本一体覆盖——拷走 `fmemos.db` 也只是拿到密文
  - 密钥：首次启动自动生成 32 字节随机数，存程序目录 `fmemos.key`（便携模式，无感解锁）
  - **密钥丢了数据无法找回**；拷库 / 同步到别的机器请连密钥文件一起带走
  - 明文库自动迁移：升级后首次启动检测到 SQLite 明文库时自动转加密
    （`sqlcipher_export` 原样搬数据），明文原件留底 `fmemos.db.plain-backup`，确认后可删
  - 密钥对不上（库被拷走 / 密钥文件被改）在启动时立即报错，绝不带病读写
- 恢复备份时校验备份文件是同密钥的加密库；**旧版明文备份不再能直接恢复**，
  会给出明确指引（用导出 / 导入迁移数据）

### 技术说明

- rusqlite 换用 `bundled-sqlcipher-vendored-openssl`（SQLCipher + 源码编 OpenSSL）；
  编译需要 perl，仓库根 `.cargo/config.toml` 已固定指向便携版 Strawberry Perl
  （解压在 `~/.workbuddy/binaries/perl-dl/`，非系统安装），并用 `OPENSSL_NO_ASM` 免去 NASM 依赖
- 密钥使用 raw-hex 形式（`PRAGMA key = "x'...'"`），跳过 PBKDF2 派生，启动零额外延迟
- 加密后每次启动用「读一次 sqlite_master」立即验证密钥，错误提前到启动而非首条业务 SQL
- 新增依赖：`getrandom`（生成密钥，Windows 走 BCrypt）；单测 +5（密钥文件往返与防篡改、
  新库落盘即密文、明文迁移数据/标签/留底、错密钥拒绝、备份文件同密钥）

## 0.9.0（2026-09-13）

### 新功能

- **JSON 备份回灌**：把 FMemos 导出的 JSON 备份重新导入，形成「导出 → 导入」的结构化备份闭环
  - **原样恢复**：每条笔记的精确时间戳、置顶状态全部照搬；标签从正文重新解析入库
  - 与既有导入共用同一套流程：按归一化正文去重（重复回灌 / 回灌到已有数据不会翻倍）、
    写入前先给条数预览、整批一个事务
  - 文件选择框与文件夹递归扫描都纳入 `.json`；结构不对的 JSON 带着文件名明确报错，
    不会被当成一条奇怪笔记吞进去

### 技术说明

- 解析纯函数 `parse_json_export`（import.rs）：只认 `app: "FMemos"` 的导出结构，
  时间戳统一过 `normalize_timestamp` 校验，脏数据退回默认值而不是报错中断
- 置顶恢复在插入后用 `UPDATE memos SET pinned_at` 补写，普通文本 / HTML 导入路径行为完全不变

## 0.8.0（2026-09-13）

### 新功能

- **图片粘贴与附件**：输入框直接粘贴或拖入图片即可（编辑已有卡片时也支持）
  - 图片字节以 BLOB 存进 SQLite（`images` 表），正文用 `![图片](image://<id>)` 引用
  - 卡片内渲染缩略图，点击全屏放大预览；待办清单等只读场景显示为【图片】占位
  - 相同图片按 sha256 自动去重，重复粘贴不占双份空间
  - 单图上限 10 MB；启动时自动清理「无引用且超过 24 小时」的孤儿图片（24 小时缓冲保护未保存的草稿图）
- 导出 Markdown 时把引用图片自动写到 `<文件名>.assets/` 目录并改写链接，导出文件自包含；
  每日 Markdown 导出同样把图片写到 `fmemos-images/`

### 技术说明

- 选 BLOB 进库而不是图片落盘：自动备份、从备份恢复、便携同步仍然只是**单个 db 文件**，
  图片天然全覆盖；新增 `base64`、`sha2` 两个直接依赖（原本就在依赖树里）
- 前端粘贴/拖拽监听挂在容器层（事件冒泡），TagInput / TagTextarea 组件零改动
- 图片字节按需懒加载（base64 → data URL），进程内缓存，卡片流反复滚动不重复请求

## 0.7.0（2026-09-13）

### 新功能

- **卡片置顶**：卡片上的「置顶」把笔记钉在列表最上方，卡片左侧出现主色竖条标识
  - 置顶区**独立于分页**：主列表只查未置顶项（`pinned_at IS NULL`），置顶项单独一次查完渲染在上方。这样 `(created_at, id)` 的 keyset 游标语义一行没改——「置顶优先」若直接混进排序，游标就不再单调，翻页必出重复或漏项
  - 改正文不会丢掉置顶状态；置顶区按最近置顶排序
- **智能列表**：把当前筛选（标签 + 搜索词 + 日期 + 无标签）存成一个命名条目，侧栏一键切回，可删除；存本地
- **导出增强**
  - 格式可选 **Markdown**（便于阅读）或 **JSON**（保留精确时间戳、置顶状态与标签数组）
  - 支持**按当前筛选导出**：勾选「只导出当前筛选」就只导出正在看的那一批，导出文件抬头会写明筛的是什么
  - 导出走 `pinned = None`（置顶项也要导出），并有回归测试守着，防止「主列表排除置顶」把导出一起带偏
- **每日 Markdown 导出**：设置里指定目录后，每天首次启动顺带写一份 Markdown 到那里（文件名带日期）。适合丢进同步盘 / 网盘——数据库快照在 WAL 下直接同步并不稳妥，Markdown 没有这个问题
  - 目标目录存在新增的 `settings` 键值表里（前端 localStorage 对后端不可见，必须入库）；目录被删或换盘时静默跳过，不阻塞启动

### 变更

- `memos` 表新增 `pinned_at` 列（老库启动时自动补列 + 建部分索引），`Memo` 结构随之多出 `pinnedAt`
- `list_memos` 新增 `pinned` 参数：`null` = 不筛、`true` = 只要置顶、`false` = 只要未置顶；旧调用不传即为原行为
- 新增 `settings` 键值表与 `get_setting` / `set_setting` 命令
- 导出命令由 `export_markdown` 改为 `export_text`（支持格式与筛选），`export_to` 增加 `format` / `filter` 参数

### 修复

- 导入 / 导出相关的浏览器假后端补齐：`set_pin`、`get_setting`、`set_setting`、`export_text`，并让 `list_memos` 支持置顶过滤与排序

## 0.6.0（2026-09-13）

### 新功能

- **待办清单**：把散在各篇笔记里的 `- [ ]` 收拢成一份清单，勾选直接回写原卡片
  - 侧栏「待办」入口，徽标显示未完成条数；状态筛选 未完成 / 全部 / 已完成
  - 分组方式可切「按标签」（取笔记第一个标签，无标签单独一组，未完成多的排在前面）或「按日期」
  - 顶部搜索框在待办视图下过滤待办文本；点 `#标签` 跳到该标签的笔记流，点「原文」定位回原卡片（卡片还在后面的分页里会自动翻页找到它）
  - 纯前端从已加载的全量笔记现算，不加后端接口；勾选回写的下标与卡片里的复选框共用同一套计数（跳过代码块），所以一定能改到正确那一行
- **数据导入**：设置 → 导入文件 / 导入文件夹，支持 flomo 导出的 HTML 与 `.md` / `.markdown` / `.txt`
  - 文件夹递归扫描；单文件里若有 `## 2021-04-05 09:43:21` 这样的分节标题按节拆成多条，没有时间的用文件修改时间
  - 按归一化正文去重（行尾归一 + 去掉首尾空白），重复导入同一个文件不会翻倍
  - 写入前先给预览：扫过几个文件、识别多少条、将新增 / 跳过重复 / 空内容各多少，并列出前几条样本，确认后才写库
  - 导入的时间戳原样保留（不用默认的 now），标签同步进 `memo_tags`，整批写入一个事务，中途失败不留半截数据

### 变更

- 设置页「从备份恢复」与「导入」共用同一条数据刷新链路（`onDataReloaded`）
- 修正侧栏「全部笔记」高亮：在回收站 / 待办视图下不再同时高亮
- 浏览器调试用假后端补上待办演示数据与导入命令桩

## 0.5.0（2026-09-13）

### 新功能

- **标签管理**：侧栏标签行 hover 出现 ⋯，支持重命名、合并到已有标签、删除
  - 重命名 / 合并连带子标签：`#读书` → `#阅读` 时，`#读书/心理学` 一并改成 `#阅读/心理学`（与标签筛选的「父标签连带子孙」语义一致）
  - 删除标签连同子标签一起从正文移除；正文只有该标签的笔记会跳过，不留下空卡片
  - 操作前显示会改写多少条笔记（含回收站里的），重命名 / 合并后当前筛选自动跟随到新标签
  - 边界与搜索一致：改 `读书` 不会误伤 `#读书笔记`；新名与原名互为上下级时直接拒绝，防自嵌套
  - 删标签会顺手吃掉相邻的一个空格，不留行首缩进或双空格
- **从备份恢复**：设置 → 「从备份恢复」列出全部自动备份（日期 + 体积），一键回滚
  - 恢复前先校验备份完整性，损坏的文件直接拒绝，绝不动现有数据
  - 恢复前自动把当前数据另存为安全副本（`before-restore-YYYYMMDD-HHMMSS.db`，保留最近 3 份）
  - 走 SQLite 在线备份 API 反向写回当前连接，立即生效、不用退出应用（Windows 下数据库文件被本进程占用，直接覆盖文件是做不到的）
  - 恢复后自动切回 WAL、补齐可能缺失的表结构，并强制重建全文索引与标签关联

### 变更

- 设置页「自动备份」区块更名为「自动备份与恢复」；备份目录解析收敛为 `commands::backup_dir()`，与打开文件夹共用

## 0.4.2（2026-09-06）

### 修复

- 单实例：应用驻留托盘时再次启动（如点桌面图标、开始菜单），直接唤出已有窗口，不再无响应

### 新功能

- 设置页底部显示当前版本号

### 变更

- favicon 圆角比例与其他平台图标统一（26%）

## 0.4.1（2026-09-06）

### 变更

- 应用图标圆角加大（约 26% 圆角半径），风格更柔和

## 0.4.0（2026-09-06）

### 新功能

- **回收站**：删除改为软删除进回收站，底部浮条 6 秒内可「撤销」；侧栏回收站入口支持恢复 / 彻底删除 / 一键清空
- **自动备份**：每天首次启动自动把完整数据库快照到程序旁 `backup/` 文件夹（SQLite 在线备份 API，WAL 下也是一致快照），保留最近 5 份
- **数据导出**：全部笔记导出为 Markdown 文件，系统另存为对话框选位置
- **设置面板**：侧栏左下角 ⚙️ 设置，外观（主题三态）、备份文件夹入口、数据导出集中管理
- **托盘常驻**：关闭窗口 = 最小化到托盘（进程不退出），托盘左键呼出/隐藏窗口，菜单提供显示/隐藏、开机自启开关、退出
- **深色模式**：浅色 / 深色 / 跟随系统三态切换，选择持久保存
- **多关键词搜索**：空格分隔多个关键词，全部命中才显示（AND 语义；任一关键词不足 3 字符时整组回退 LIKE）
- **搜索结果高亮**：命中的关键词在卡片中用高亮标出（不区分大小写，代码与链接内不高亮）
- **输入框自动增高**：编辑区随内容长高（封顶半屏），超出后内部滚动且滚动条隐藏
- **那年今日回顾**：往年同月同日的笔记可一键回顾（可换一条），弹窗内可直接跳到对应卡片编辑

### 变更

- **项目更名为 FMemos**：安装包名、窗口标题、界面 logo、进程名、应用标识符（`com.ray.fmemos`）、数据文件名（`fmemos.db`）、项目目录名（`fmemos/`）全部同步更改；旧版 `flomo.db` 如需保留可手动改名为 `fmemos.db` 放到 exe 旁继续使用
- 项目接入 GitHub 仓库；安装包发布方式改为 GitHub Releases（仓库内不再携带二进制）

## 0.3.1（2026-09-05）

### 修正

- 退出列表：空标记项按 `Enter` 只删除标记，光标留在当前空行（0.3.0 会跳到上一行行尾）
- 光标跟随：程序化移动光标（列表回车延续、补全标签）后，编辑框内部滚动与页面滚动自动跟随，光标行始终保持可见（含软换行的精确测量）

## 0.3.0（2026-09-05）

### 新功能

- **列表输入辅助**：在 `- `、`1. `、`- [ ]` 列表行按 `Enter` 自动补下一行标记（有序列表自动递增，新任务始终未完成，行中回车拆分条目）；空标记项再按 `Enter` 退出列表（连同空行删除，光标落回上一行行尾）；`Shift + Enter` 强制普通换行。新建输入框与卡片编辑框均生效

## 0.2.0（2026-09-05）

性能与健壮性优化 + 新功能，无破坏性变化。

### 新功能

- **TODO 复选框**：正文里 `- [ ] 任务` / `- [x] 任务` 渲染为可点击的复选框列表，点击直接改写正文并保存；支持大写 `[X]`、空文本项；代码块内的行不解析、不计入序号
- **草稿自动保存**：输入框未发送的内容实时存 localStorage，重启或误关窗口后自动恢复，发送成功后清除

### 优化

- 数据流：筛选 / 搜索不再重拉全量数据（全量列表只在增删改后刷新）；搜索输入 150ms 防抖，并按请求序号丢弃过期响应，快速输入不会出现旧结果覆盖新结果
- 渲染：App 侧全部回调 `useCallback` 稳定化，Sidebar / MemoCard / Editor 包 `React.memo`，卡片 Markdown 渲染按内容记忆化——搜索输入时卡片流与侧栏不再整体重渲染
- 分页：卡片流按 50 条一页 keyset 分页（游标 `(created_at, id)`），滚动到底自动加载下一页；后端 `list_memos` 增加 `limit` / 游标参数（钳制 1..=500）
- 启动：派生数据（FTS 索引、标签关联）从每次启动全量重建改为按 `PRAGMA user_version` 版本门控，版本不变时跳过
- 搜索：两字以内的 LIKE 回退对 `%` `_` `\` 做转义（`ESCAPE '\'`），搜 "100%" 之类不再误匹配
- 事务：新建 / 更新 / 删除的写库与标签关联包进同一事务，中途失败不再留下不一致
- 错误处理：新建 / 保存 / 删除失败时在顶部横幅提示，输入框保留内容、卡片保留编辑状态；更新不存在的 memo 返回明确错误
- 其他：标签补全高亮下标越界防护；favicon 换成应用同款 FM 图标；标签树 / Markdown 解析补充单测；Tauri CSP 从 null 收紧为 `default-src 'self'`（IPC 与开发服务器白名单放行）

## 0.1.0（2026-09-04）

首个打包版本。

- 卡片笔记 CRUD：新建 / 编辑 / 删除，数据存本地 SQLite（便携模式，库文件与 exe 同目录）
- `#标签` 语法：正文自动解析标签，侧栏层级树展示与计数；`memo_tags` 表精确匹配，父标签连带子孙（`#读书` 命中 `#读书/心理学`，不误命中 `#读书笔记`）
- 标签自动补全：输入框与卡片编辑框输入 `#` 弹出已有标签建议，↑↓ 选择、Enter/Tab 确认、Esc 关闭
- 全文搜索：FTS5 trigram 分词，≥3 字符任意子串可命中（中英文）；两字以内自动回退 LIKE
- 日期分组与统计：卡片流按日期分组（今天 / 昨天 / M月D日 周X · N 条）；侧栏近 15 周热力图，点击某天筛选
- 回顾：随机回顾（可换一条）/ 每日回顾（同一天固定一条）
- Markdown 渲染：**加粗**、`行内代码`、代码块、有序 / 无序列表；网址自动识别为链接，点击用系统浏览器打开
- 全局快捷键 `Ctrl+Shift+M`：随时呼出 / 隐藏窗口，呼出后自动聚焦输入框
- 应用图标：FM 字母组合（绿色底白色连字）
